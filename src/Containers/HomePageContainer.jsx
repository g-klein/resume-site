import React, { Component } from 'react';

export class HomePageContainer extends Component {
    render() {
        return(
            <nav>
                <a href="#">Home link</a>
            </nav>
        );
    }
}